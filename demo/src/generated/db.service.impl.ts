import { DB } from "../user";

// Generated by AutoGen...
import { User } from "../user";

export class DBImpl extends DB {
  // private helper method to save user to users map
  private _save(user: User): void {
    this.users.set(user.name, user);
  }

  // private helper method to find user from users map
  private _find(name: string): User | undefined {
    return this.users.get(name);
  }

  save(user: User): void {
    this._save(user);
  }

  find(name: string): User | undefined {
    return this._find(name);
  }
}