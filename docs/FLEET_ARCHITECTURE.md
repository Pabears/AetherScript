# AetherScript 舰队架构

> "单打独斗是 1.0，五巨头群殴才是 2.0。"

## 设计理念

AetherScript 舰队版将原来的单 agent 流程升级为**多角色协作系统**。每个阶段都有专属角色参与，通过真实的观点碰撞和多轮 PK，产出比单一视角更高质量的结果。

---

## 模型分配（锁定版）

| 角色 | 模型 | Temperature | 理由 |
|------|------|-------------|------|
| 🤪 超级狂野PM | Gemini 3.1 Pro | 1.4 | 脑洞发散，创意爆炸 |
| 👑 架构暴君 | Claude Opus 4.6 | 0.3 | 严密推理，不容模糊 |
| 🔍 QA黑客 | Claude Opus 4.6 | 0.5 | 系统性找漏洞 |
| 🔒 安全死心眼 | Claude Opus 4.6 | 0.2 | 越保守越好 |
| 💰 预算狂魔 | Claude Opus 4.6 | 0.4 | 成本分析需要严谨 |
| 🏗️ 稳健派开发 | Claude Sonnet 4.6 | 0.3 | 够用不浪费 |
| ⚡ 性能派开发 | Gemini 3.1 Pro | 0.7 | 激进发散，极致性能 |
| 🎨 优雅派开发 | Claude Opus 4.6 | 0.5 | 最精细的代码品味 |

---

## 五大核心角色

### 🤪 超级狂野PM (pm)
- **人格**：脑洞无限大，挖需求像剥洋葱
- **模型**：Gemini 3.1 Pro @ temperature=1.4
- **口头禅**：「用户真正的痛点是什么？」「10倍大是什么样子？」
- **职责**：隐藏需求、用户故事、商业价值、边界扩展
- **触发词**：需求模糊、用户说不清楚、"我想要一个..."

### 👑 架构暴君 (arch)
- **人格**：傲慢、精准，对模糊设计零容忍
- **模型**：Claude Opus 4.6 @ temperature=0.3
- **口头禅**：「这个方案会在生产环境爆炸」「你的数据流图呢？」
- **职责**：技术可行性、系统设计、依赖关系、扩展性、性能审判
- **触发词**：架构讨论、性能问题、系统设计

### 🔍 QA黑客 (qa)
- **人格**：快乐来自找到别人没想到的边界条件
- **口头禅**：「如果用户做了最蠢的事会怎样？」「并发场景呢？」
- **职责**：边界条件、异常场景、功能测试、可测试性评估
- **触发词**：测试覆盖、边界情况、异常处理

### 🔒 安全死心眼 (sec)
- **人格**：假设所有用户都是攻击者，所有输入都是恶意的
- **口头禅**：「这种情况一定会被人尝试」「输入验证呢？」
- **职责**：安全漏洞、权限边界、OWASP Top 10、安全契约
- **触发词**：安全审计、权限控制、数据保护

### 💰 预算狂魔 (budget)
- **人格**：热爱简单，憎恨复杂，每行代码都是成本
- **口头禅**：「这真的值得吗？」「有没有更便宜的方式？」
- **职责**：成本估算、复杂度控制、ROI、MVP边界、技术债
- **触发词**：范围讨论、资源限制、优先级

---

## 角色组合

| 组合 | 成员 | 用途 |
|------|------|------|
| **三巨头** | arch + qa + sec | 技术审判、架构评审、实现选拔 |
| **五巨头** | 全部五个角色 | PRD 拷问、最终放行 |
| **开发三组** | 稳健派 + 性能派 + 优雅派 | 并行实现 PK |

---

## aesc-pre 舰队流程

```
用户输入原始需求
        ↓
┌─────────────────────────────────────────┐
│           阶段一：PRD 拷问（五巨头）         │
│                                         │
│  pm → arch → qa → sec → budget         │
│  每人提 3-5 个刁钻问题 + 点评前序观点      │
│              ↓                          │
│        用户逐一回答                      │
│              ↓                          │
│         10 轮 PK                        │
│  (每轮：汇总观点 → 三巨头投票风险排序)     │
│              ↓                          │
│        输出 docs/PRD.md                 │
└─────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────┐
│         阶段二：架构设计（三巨头）          │
│                                         │
│  arch + qa + sec 各自独立出方案          │
│              ↓                          │
│         10 轮 PK                        │
│  (互相质疑：可扩展性/可测试性/安全契约)    │
│              ↓                          │
│    输出 src/[Name].abstract.ts          │
│    (含 // @autogen 和详细 JSDoc)        │
└─────────────────────────────────────────┘
```

### PRD 文件格式

```markdown
# PRD: [项目名]

## 背景与目标
## 用户故事（核心 1-2 个）
## 功能需求（MVP）
## 非功能需求
- 性能：[arch 定义]
- 安全：[sec 红线]
- 可测试性：[qa 要求]
## 排除范围
## 风险与决策记录
```

---

## aesc-gen 舰队流程

```
读取 Abstract Class（// @autogen）
        ↓
┌─────────────────────────────────────────┐
│         三支开发团队（并行实现）            │
│                                         │
│  🏗️ 开发一组（稳健派）                   │
│     优先：可维护性、完整错误处理、清晰命名  │
│                                         │
│  ⚡ 开发二组（激进性能派）                │
│     优先：Promise.all、缓存、减少 IO     │
│                                         │
│  🎨 开发三组（优雅代码派）                │
│     优先：设计模式、纯函数、零重复代码     │
└─────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────┐
│           三巨头审判                     │
│                                         │
│  arch 评分维度：可扩展性40% + 性能20%...  │
│  qa  评分维度：可测试性40% + 覆盖20%...  │
│  sec 评分维度：安全性40% + 权限20%...    │
│                                         │
│  加权平均最高者胜出                      │
│  如最高分 < 60 → 要求融合或重新实现      │
└─────────────────────────────────────────┘
        ↓
   输出 src/generated/*.impl.ts
   输出 src/generated/container.ts
```

### 审判评分维度

| 维度 | arch 权重 | qa 权重 | sec 权重 |
|------|-----------|---------|---------|
| 可扩展性 | 40% | 10% | 10% |
| 可测试性 | 10% | 40% | 10% |
| 安全性 | 10% | 10% | 40% |
| 代码质量 | 20% | 20% | 20% |
| 性能 | 20% | 20% | 20% |

---

## aesc-test 舰队流程

```
读取实现文件（src/generated/）
        ↓
┌─────────────────────────────────────────┐
│         三个测试团队（并行）               │
│                                         │
│  🔍 QA黑客 → 质量/功能/边界测试          │
│  🔒 安全死心眼 → 安全/注入/权限测试      │
│  👑 架构暴君 → 性能/并发/内存测试        │
└─────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────┐
│           五巨头终极放行                  │
│                                         │
│  每人投票：✅通过 / ⚠️有条件 / ❌不通过  │
│                                         │
│  全部 ✅ → LGTM，输出 test/             │
│  有 ⚠️ → 修改后重提                    │
│  任意 ❌ → 对应团队重写                 │
└─────────────────────────────────────────┘
        ↓
   输出 test/*.test.ts
   输出 LGTM 报告
```

---

## PK 轮次规则

### 10轮 PK 结构

```
第 1-3 轮：发散
  各角色独立表达观点，不受约束

第 4-7 轮：碰撞
  对前序观点表态（同意/反对/补充）
  识别核心分歧点，重点攻击

第 8-10 轮：收敛
  三巨头投票每个议题
  预算狂魔给 MVP 边界
  整合产出文档
```

### 胜出判定
- 三巨头加权平均分最高者胜出
- 并列时 arch 有决定权
- 最高分 < 60 分时，要求融合方案或全部重来

---

## 与原版对比

| 维度 | 原版（单 agent） | 舰队版（多角色） |
|------|----------------|----------------|
| aesc-pre | 1个 agent 提问 | 5个角色轮流拷问 + 10轮PK |
| aesc-gen | 1份实现 | 3组并行 + 三巨头审判 |
| aesc-test | 1份测试 | 3维测试 + 五巨头放行 |
| 质量保证 | 单视角 | 多维度互相制衡 |
| 输出质量 | 依赖单次提示质量 | 通过PK收敛至最优 |

---

## 实战经验记录

> 本节记录舰队在真实运行中发现的问题和改进方案。每次流程迭代都应在此留下痕迹。

---

### 实验一：三巨头评审 aesc-gen 实现（2026-03-04）

**背景：** AetherScript 2.0 自我重写任务。三组开发团队并行生成7个类的实现后，启动三巨头评审。

#### 发现的问题

**问题1：三巨头"大锅炖"导致 output 截断**

第一轮做法：arch/qa/sec 各自一个 sub-agent，每人评审全部7个类。

结果：每个 sub-agent 都跑了2-3分钟，输出到一半就被 token 上限截断。报告不完整，后几个类的评审缺失。

根本原因：**output 有硬上限，input 相对便宜**。让一个 agent 输出7个类的完整评审，output 必然超限。

**改进方案：充分利用 input/output 长度不对称**

每个类单独一个 sub-agent，三巨头视角合并在一个 prompt 里输出。

- Input：塞进3个实现文件 + 技术设计文档（便宜）
- Output：只输出这一个类的裁决，严格限600字（可控）
- 并行：7个 sub-agent 同时跑

结果：28~62秒一个类，全部完整，无截断。**7次调用 vs 3次截断调用，质量翻倍，时间更短。**

**原则：output 是稀缺资源，要切细切小；input 是廉价资源，可以大方投入。**

---

**问题2：Technical Design 生成时 write 调用被截断**

做法：sub-agent 生成+写入一体，一次调用生成4万token的设计文档并写入文件。

结果：token 满了，write 调用在 content 参数中间被截断，文件没有落地。跑了两个 sub-agent，10分钟，颗粒无收。

改进方案：**生成和写入分离。** 主 session 直接分段调用 write 工具，不依赖 sub-agent 写大文件。

---

#### 改进后的三巨头评审流程（已验证）

```
三组开发团队并行实现（稳健派/性能派/优雅派）
        ↓
┌─────────────────────────────────────────────┐
│  7个类并行裁决（每类一个 sub-agent）           │
│                                             │
│  每个 sub-agent 的 prompt：                 │
│  - Input: 3个实现文件 + 接口定义（大方投入）  │
│  - 角色: 三巨头合体（arch + qa + sec）       │
│  - Output: 严格限600字，固定格式             │
│    - 评分表（3×3）                          │
│    - 各方案优缺点（bullet，3-5条）           │
│    - 最终选型                               │
│    - 必须融合内容                           │
│    - 阻塞级安全问题                         │
└─────────────────────────────────────────────┘
        ↓
   主 session 收集7份结果 → merge 写入 VERDICT.md
```

**关键参数：**
- 模型：Opus 4.6（三巨头审判不省）
- 并行数：7（类数量）
- Output 限制：600字/类（严格执行，格式锁死）
- 总耗时：~60秒（最慢的那个）

---

### 实验二：Technical Design 生成流程问题（2026-03-04）

**背景：** aesc-pre 完成后，需要生成 TECHNICAL-DESIGN.md。

#### 发现的问题

**做法1（失败）：** 三巨头各自独立设计8个 Abstract Class，输出到同一文件。

结果：sub-agent 生成内容超大，write 调用被截断，文件没落地。

**改进方案（待验证）：**

方案A — **分文件输出，再合并**

```
架构暴君先设计整体架构（输出 index.md）
        ↓
每个类单独一个文件（class1.md, class2.md...）
        ↓
QA黑客 + 安全死心眼 分别评审每个文件
        ↓
三巨头合力修订所有文件
        ↓
主 session 合并成 TECHNICAL-DESIGN.md
```

方案B — **架构暴君先行，其余评审**

```
架构暴君独立设计完整 Abstract Class 体系
        ↓
QA黑客 + 安全死心眼 并行评审（各输出 review.md）
        ↓
三人合力对每个文件逐一修改
```

**核心原则：** 每个 sub-agent 只负责一个文件的内容，output 始终可控。

---

### 经验总结

| 场景 | 踩过的坑 | 正确姿势 |
|------|---------|---------|
| 多类评审 | 一个 agent 评全部 → 截断 | 每类一个 agent，output 限字数 |
| 大文件生成 | sub-agent 生成+写入 → 截断 | 主 session 分段写入，生成写入分离 |
| 文档设计 | 三巨头同时生成一份大文档 → 截断 | 每人负责子文件，最后 merge |
| Token 分配 | input/output 对等 | input 大方投，output 切细切小 |

**核心洞察（时恒提出）：** 充分利用 input 和 output 的长度不对称。input 便宜，output 珍贵——把所有背景塞进 input，把 output 切成最小有效单元。
